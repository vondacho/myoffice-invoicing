package edu.noia.myoffice.invoicing.domain.service;

public interface BillingManager {
/*
    Reception receive(Income income, Customer customer);
    Reception receive(Income income, Folder folder);
    Reception receive(Income income, Ticket ticket);
    Reception receive(Income income, Ticket ticket, Customer customer);

    Invoice invoice(Folder folder);
    void save(Invoice invoice);
    void close(Invoice invoice);
    void reopen(Invoice invoice);
    Recall recall(Invoice invoice);
    void delete(Invoice invoice);
    */
}
