package edu.noia.myoffice.invoicing.domain.service;

public class DataService {
/*
    @Autowired
    private InvoiceRepository invoiceRepository;

    @Autowired
    private RequestRepository requestRepository;

    public List<Debt> findAllDebts(FolderId folderId) {
        return new ArrayList<>(invoiceRepository.findAll(
                Specifications.where())).addAll(requestRepository.findOpenByFolder())
    }

    public Debt save(Debt debt) {
        return debt instanceof Invoice ? invoiceRepository.save((Invoice)debt) : requestRepository.save((Request)debt);
    }

    public List<SaleItem> findAllFees(FolderId folderId) {
        return Collections.emptyList();
    }

    public List<Provision> findAllProvisions(FolderId folderId) {
        return Collections.emptyList();
    }
    */
}
